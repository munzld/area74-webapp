import { UpcomingConferencesPipe } from './upcoming-conferences.pipe';

describe('UpcomingConferencesPipe', () => {
  it('create an instance', () => {
    const pipe = new UpcomingConferencesPipe();
    expect(pipe).toBeTruthy();
  });
});
