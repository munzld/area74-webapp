import { PastConferencesPipe } from './past-conferences.pipe';

describe('PastConferencesPipe', () => {
  it('create an instance', () => {
    const pipe = new PastConferencesPipe();
    expect(pipe).toBeTruthy();
  });
});
